/**
 * Re-export navigation metadata from the canonical config.
 * Kept for backwards-compatibility with existing imports across the frontend.
 */
export {
  NAVIGATION_ITEMS,
  SIDEBAR_ITEMS,
  type SidebarItemId,
  type NavigationItem,
  GROUP_LABELS,
} from './config/navigation';
